/**
 * Adds two numbers and returns their sum
 *
 * @a number - addend 1
 * @b number - addend 2
 *
 * @example
 *
 * myFunction(1, 2)
 * // => 3
 */
const myFunction = (a: number, b: number) => {
  return a + b;
};
